{
  "name": "degate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ degate/degate",
    "deploy-dev": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ degate/degate",
    "create-local": "graph create --node http://localhost:8020/ degate/degate",
    "remove-local": "graph remove --node http://localhost:8020/ degate/degate",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 degate/degate"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.59.0",
    "@graphprotocol/graph-ts": "^0.34.0"
  }
}
